<Window x:Class="Spi.Spielwiese.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Spi.Spielwiese"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1"
                    Orientation="Vertical"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <Button x:Name="btnSimpleBinding" Content="Simple Binding" Padding="10" Margin="10" Click="btnSimpleBinding_Click" />
            <Button x:Name="btnObjectBinding" Content="Object Binding" Padding="10" Margin="10" Click="btnObjectBinding_Click"/>
            <Button x:Name="btnListBinding" Content="List Binding" Padding="10" Margin="10" Click="btnListBinding_Click"/>
            <Button x:Name="btnTemplateBinding" Content="Template Binding" Padding="10" Margin="10" Click="btnTemplateBinding_Click"/>
            <Button x:Name="btnMVVM" Content="MVVM Beispiel" Padding="10" Margin="10" Click="btnMVVM_Click"/>
        </StackPanel>

    </Grid>
</Window>
